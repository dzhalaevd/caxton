Add declarative grouped reports and pivot-like matrices with arbitrary Python
aggregation callables, filtered and multi-input aggregates, hierarchical merge
ranges, strict typed dimension keys, empty-scope defaults, typed matrix columns,
deterministic ordering, and single-pass buffering for one-shot sources.
